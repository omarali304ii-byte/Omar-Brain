# v14 Living Agent Learning OS — Build Report

## Added
- deterministic agent entry protocol,
- per-agent current cognitive stack,
- exact restart contract,
- failure-to-immunity loop,
- triggered learned rules,
- failure pattern registries,
- regression/eval registries,
- freshness state,
- immunity registry,
- per-agent runtime state,
- constrained specialist write hooks,
- constrained mutating Bash hook,
- Claude subagent project memory,
- project-council skill v2,
- future-project scaffold v2,
- council validator and start-brief builder.

## Meta Word of Mouth bootstrap
All specialist and control roles received project-specific domain models, owned surface maps, current findings, learned-rule candidates, failure patterns, eval registries, unknowns and exact restart pointers based on the stored repo-verified assessment at revision `8c027fabf85fe46fa0395eb459c0289872fef491`.

## Truth limit
The v14 build does not claim the stored revision is still current. Every role is explicitly marked partial until the live repository is re-inspected.

## Recurrence claim
The system increases practical recurrence resistance through triggers and evals. It does not claim mathematical impossibility that a problem can recur.
