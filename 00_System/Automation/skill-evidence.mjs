#!/usr/bin/env node
import fs from 'node:fs'; import path from 'node:path';const [vaultArg,skill_id,project_or_episode,verdict,...noteParts]=process.argv.slice(2);if(!vaultArg||!skill_id||!project_or_episode||!verdict){console.error('Usage: skill-evidence.mjs <vault> <skill-id> <project-or-episode> <SUCCESS|PARTIAL|FAIL|INVALIDATED> "note" [--evidence path]');process.exit(2)}
const allowed=new Set(['SUCCESS','PARTIAL','FAIL','INVALIDATED']);if(!allowed.has(verdict)){console.error('Invalid verdict');process.exit(2)}const vault=path.resolve(vaultArg);const reg=JSON.parse(fs.readFileSync(path.join(vault,'00_System','Skill OS','skill-registry.json'),'utf8'));if(!reg.skills.some(s=>s.skill_id===skill_id)){console.error('Unknown skill_id');process.exit(1)}
let note=noteParts.join(' ');let evidence_path=null;const m=note.match(/\s--evidence\s+(.+)$/);if(m){evidence_path=m[1].trim();note=note.slice(0,m.index).trim()}const rec={ts:new Date().toISOString(),skill_id,project_or_episode,verdict,note,evidence_path};fs.appendFileSync(path.join(vault,'00_System','Skill OS','skill-evidence.jsonl'),JSON.stringify(rec)+'\n');console.log(JSON.stringify(rec,null,2));
